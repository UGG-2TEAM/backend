spring:
  datasource:
    url: jdbc:mysql://uggdb.c9ocs4kg22yt.ap-northeast-2.rds.amazonaws.com:3306/uggDB
    username: admin
    password: gbvsfy7z!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kakao:
    auth:
      client: ${KAKAO_CLIENT}
      redirect: ${KAKAO_REDIRECT}

jwt:
  expiration_time: 86400000
  secret: ${JWT_SECRET}

server:
  port: 8080
